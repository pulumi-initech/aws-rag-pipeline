{
    "name": "aws-rag-pipeline",
    "main": "index.ts",
    "scripts": {
        "test": "mocha 'test/**/*.test.ts' --require ts-node/register --timeout 10000",
        "test:watch": "mocha 'test/**/*.test.ts' --require ts-node/register --watch --timeout 10000",
        "test:coverage": "nyc mocha 'test/**/*.test.ts' --require ts-node/register --timeout 10000"
    },
    "devDependencies": {
        "@types/node": "^18",
        "@types/mocha": "^10.0.0",
        "@types/chai": "^4.3.0",
        "typescript": "^5.0.0",
        "mocha": "^10.2.0",
        "chai": "^4.3.10",
        "sinon": "^17.0.1",
        "nyc": "^15.1.0",
        "ts-node": "^10.9.0"
    },
    "dependencies": {
        "@pinecone-database/pulumi": "^0.4.3",
        "@pulumi/aws": "^6.0.0",
        "@pulumi/awsx": "^2.0.2",
        "@pulumi/pulumi": "^3.113.0"
    }
}
